apiVersion: v1
kind: Service
metadata:
  name:  {{ .Chart.Name }}
  labels:
    app: metrics-app
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: metrics-app
